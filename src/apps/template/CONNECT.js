/**
 * COPY-KIT ONLY — not mounted, not in the launcher, not seeded.
 *
 * Full walkthrough:
 *   frontend/readme/app-flow/CREATE-APP-FROM-TEMPLATE.md
 *
 * Find-replace (case-sensitive, this order):
 *   template_record  → first module slug (optional)
 *   template         → app key (lowercase, no hyphens)
 *   Template         → display name
 *   TEMPLATE         → constant prefix
 */

export const APP_KEY = "template";
export const APP_LABEL = "Template";
